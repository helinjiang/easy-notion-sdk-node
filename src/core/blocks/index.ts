import { extendAPI } from './extend-api';
import { reqTypes } from './req-types';

import * as IBlocksTypes from './types';

export { IBlocksTypes };

export const blocks = {
  extendAPI,
  reqTypes,
};